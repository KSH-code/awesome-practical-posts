const condition = true
if (condition) {

} else if () {

} else if () {

}

// 일단 충격적이게도 js 에는 else if 라는 키워드가 존재하지 않는다.
// 실행문이 하나인 경우 {} 를 생략해도 되기 때문에 
// if { } else { if { } else { if }} 와 같이 선언되는것과 동일하다.